import './TitleBar.css';


const TitleBar = (props) => {
    return ( 
        <div id='title-bar'>
            <label id='title-bar-label'>Music Library</label>
        </div>

    );
}
 

export default TitleBar;
